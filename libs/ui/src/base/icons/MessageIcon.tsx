/* eslint-disable-next-line */
export interface MessageIconProps {}

export function MessageIcon(props: MessageIconProps) {
  return (
    <div>
      <h1>Welcome to MessageIcon!</h1>
    </div>
  );
}

export default MessageIcon;
