import Header from "@CONTAINERS/Header";

const HomeHeader = () => {
  return (
    <Header
      left={null}
      center={<div className={``}>Proclaimer</div>}
      right={null}
    />
  );
};

export default HomeHeader;
